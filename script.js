window.onload(init);

function init () {

    var list = ["goat", "rat", "hen"];

    var check = true;

    for(var i = 0; i = list.length; i ++){
        if(check){
            console.log("you've do it")
        }
    }
}